<?php

declare (strict_types = 1);

define('RELIVE', 16);
define('LOOPS', 4096);
define('EDGE', 80.0);

$start_time = microtime(true);

function replay(
    float $abscissa,
    float $ordinate
): int {
    return 0;
}

function mandelbrot(
    mixed $start_time
): void {}
